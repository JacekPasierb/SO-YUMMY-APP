export const selectOwnRecipes = (state) => state.ownRecipes.ownRecipes;
export const selectTotalOwnRecipes = (state) => state.ownRecipes.totalOwnRecipes;
export const selectIsLoading = (state) => state.ownRecipes.isLoading;
