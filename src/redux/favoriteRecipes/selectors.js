export const selectFavoriteRecipes = (state) => state.favoriteRecipes.favoriteRecipes;
export const selectIsLoading = (state) => state.favoriteRecipes.isLoading;
export const selectIsError = (state) => state.favoriteRecipes.error;
